spring:
  application:
    name: ProductService
  datasource:
    url: jdbc:mysql://localhost:3306/sbms26_ecombackend
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
server:
  port: 9090